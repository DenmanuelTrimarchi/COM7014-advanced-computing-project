# Runtime dependencies, pinned to the exact versions the published results were
# produced under. The programme refuses to report an evaluation as evidence if
# an installed version differs (see check_dependency_contract).
#
# PyYAML was removed at the open-set revision: it is imported by neither the
# programme nor any test. matplotlib returned with Experiment 7, which
# generates the dissertation figures. Test-only packages live in
# requirements-dev.txt.
numpy==2.5.1
opencv-python-headless==4.13.0.92
Pillow==12.3.0

# Experiment 7: the review classifier and its figures. Neither the five
# baseline experiments nor the self-tests import them.
scikit-learn==1.9.0
matplotlib==3.11.1

# Used only by the optional local human-review interface (--mode review). The
# five-experiment evaluation and the self-tests never import it.
streamlit==1.60.0
